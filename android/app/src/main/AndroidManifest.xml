<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Update check: the only network access in the app, and only ever when
         the user taps "Check for updates". -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Persistent privileged access: once granted via Shizuku/root (or
         `adb shell pm grant`), enables display resolution/density switching
         through the WindowManager without needing Shizuku again. -->
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />

    <!-- Shizuku API: lets the app run privileged shell commands. -->
    <uses-permission android:name="moe.shizuku.privileged.api.permission.API" />

    <!-- Optional: keep the app from being killed while applying profiles. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Force Landscape: an invisible overlay window whose layout params request
         landscape, which overrides apps that lock themselves to portrait (e.g.
         the HyperOS launcher). Requires "Display over other apps". -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Force Landscape: persist accelerometer_rotation/user_rotation so the
         display returns to landscape even if an app briefly forces portrait.
         Requires the "Modify system settings" special access. -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />

    <!-- Foreground service: watches for external displays and applies the
         saved profile automatically when one connects. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:label="MoniTune"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:largeHeap="true"
        android:enableOnBackInvokedCallback="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Shizuku Provider -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERNET" />

        <!-- Auto-apply: watches display adds/removes in the background. -->
        <service
            android:name=".display.ExternalDisplayService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="external_display_automation" />
        </service>

        <!-- Force Landscape: invisible orientation-forcing overlay. -->
        <service
            android:name=".display.OrientationOverlayService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="orientation_lock" />
        </service>

        <receiver
            android:name=".display.BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
        <!-- Required on Android 11+ for canLaunchUrl/launchUrl to see a browser:
             the Shizuku setup guide and the update release page. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>
</manifest>
